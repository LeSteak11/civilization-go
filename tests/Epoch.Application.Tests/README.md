# Epoch.Application.Tests

Populated at **M3** (Snapshot and replay): same-seed offers and layout, full replay hash
equality, normalized side-swap fixtures, corrupted and version-mismatched snapshots.

Deliberately not a wired project yet. A suite that asserts nothing is worse than no
suite, because it reports green.
