# Epoch.Performance.Tests

Populated at **M1/M5**: 1,000 headless matches inside an owner-approved CI and device
budget, with no allocation regressions in the per-turn hot path once correctness is
stable (Technical Plan sec.9.1). The budget itself is owner decision sec.16 item 10 and
is measured before it is chosen.
